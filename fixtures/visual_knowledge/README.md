# Visual Knowledge fixtures

These synthetic public-safe examples exercise the v0.4 semantic boundary.

- `reference-role.example.json` allows only line/style influence and cannot alter
  identity.
- `retrieval-query.example.json` binds task, dimensions, roles, result limit and an
  explicit rights-class allowlist.

The fixtures contain no image bytes, local paths, Provider implementation, external AI
identity, universal aesthetic claim or authority to evaluate/promote/merge/deploy.
